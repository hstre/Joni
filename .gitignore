__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ruff_cache/
.venv/
venv/
.env
*.log
.DS_Store
.joni/
joni.json

_desi/

# fast-load snapshot sidecar (git-ignored cache; the committed layer9.json journal is the source of
# truth). Survives `git reset --hard` as an untracked file so cycles after the first skip replay.
state/*.snapshot.json

# Layer 9 v2 store + SQLite sidecars — a generated, rebuildable cache (joni-layer9-convert), not
# the source of truth. Never committed.
state/*.sqlite
state/*.sqlite-wal
state/*.sqlite-shm

# generated, rendered on demand from FORSCHUNGSTAGEBUCH.md
FORSCHUNGSTAGEBUCH.pdf
