["Support a local, uncommitted .agents/agents.local.md for user-specific agent instructions (#713)", "Lazy MemorySpool plus a faster spool load path: managing dataframes are built only when needed, directory reads go straight through the file's FiberIO, and merges stream into a pre-allocated array — much faster reading/merging of many (hundreds of) files with ~45% lower peak merge memory (#738)", "Lazily import matplotlib and scipy.signal so import dascore is"]