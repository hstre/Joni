["Self-attention's pairwise token interactions scale quadratically with sequence length, making it the most expensive part of the Transformer at long context lengths.", "Standard dense attention applies the same set of attention heads to every token regardless of token difficulty or information content, wasting compute.", "We propose Grouped Query Experts (GQE), a mixture-of-experts layer on top of grouped-query attention (GQA), where a router selects k query-head experts per token within each GQA group while all key-value (KV) heads remain dense and unchanged.", "On a fixed 30B token budget at the 250M parameter scale, GQE matches the all-active GQA baseline in downstream accuracy while activating half the query heads per token."]